<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="renderer" content="webkit">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= page_title %></title>

    <%= stylesheet_link_tag    'frontend', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'frontend', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
</head>

<body id="<%= page_id %>">

    <div class="hidden-xs">
        <div class="wrapper">
            <%= yield %>
            <div id="mask"></div>
        </div>

        <%= render 'layouts/frontend/nav' %>

        <div id="sidebar">
            <% if current_user %>
                <div id="sidebar-profile">
                    <div class="profile-header">
                        <img src="<%= current_user.avatar.url %>" width="100" height="100" class="avatar">
                        <p class="name"><b><%= current_user.email %></b></p>
                    </div>
                    <div class="profile-links">
                        <a href="/orders">
                            <span class="glyphicon glyphicon-home"></span> 用户中心</a>
                        <a href="/orders">
                            <span class="glyphicon glyphicon-list-alt"></span> 我的订单</a>
                        <a href="/users/profile">
                            <span class="glyphicon glyphicon-user"></span> 账户资料</a>
                        <a href="/sign_out" data-method="delete">
                            <span class="glyphicon glyphicon-log-out"></span> 安全退出</a>
                    </div>
                </div>
            <% else %>
                <div id="sidebar-signup">
                    <%= render 'frontend/users/registrations/form' %>
                </div>
                <div id="sidebar-signin">
                    <%= render 'frontend/users/sessions/form' %>
                </div>
            <% end %>
        </div>
    </div>

    <div class="phone-tips visible-xs-block">
        <span class="glyphicon glyphicon-phone"></span>
        <p>暂不支持手机<br>请使用电脑访问本站</p>
    </div>

    <script type="text/javascript" id="gon">
        <%= include_gon(:need_tag => false) %>
    </script>
</body>
</html>
