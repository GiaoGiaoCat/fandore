= simple_form_for([:admin, @reply], url: dynamic_url_for_reply(@comment, @reply), html: { class: 'form-horizontal' }) do |f|
  = f.input :title, wrapper: :horizontal_form
  = f.input :comment, wrapper: :horizontal_form

  .hr-line-dashed
  .form-group
    .col-sm-4.col-sm-offset-2
      = link_to 'Cancel', :back, class: 'btn btn-white'
      = f.submit class: 'btn btn-primary'
