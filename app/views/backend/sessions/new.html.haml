.middle-box.text-center.loginscreen.animated.fadeInDown
  %div
    %div
      %h1.logo-name IN+
    %h3 Welcome to IN+
    %p
      Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
    %p Login in. To see it in action.
    = form_for(@sign_in, url: admin_sign_in_path) do |f|
      - if @sign_in.errors.any?
        .error_messages
          %h2 Form is invalid
          %ul
            - for message in @sign_in.errors.full_messages
              %li= message
      .form-group
        = f.text_field :username, autofocues: true, class: 'form-control', placeholder: '手机号/邮件'
      .form-group
        = f.password_field :password, class: 'form-control', placeholder: '密码'
      = f.submit 'Login', class: 'btn btn-primary block full-width m-b'
      = link_to '密码找回', users_forget_password_path, class: 'btn btn-sm btn-white btn-block'

    %p.m-t
      %small Inspinia we app framework base on Bootstrap 3 &#169; 2015
