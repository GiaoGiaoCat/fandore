.middle-box.text-center.loginscreen.animated.fadeInDown
  %div
    %div
      %h1.logo-name IN+
    %h3 Welcome to IN+
    %p
      Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
    %p Login in. To see it in action.
    = form_for(@activation, url: activations_path) do |f|
      .form-group
        您要绑定的邮箱是
        = @activation.to
      .form-group
        = f.hidden_field :code, value: params[:code], autofocues: true, class: 'form-control'
      = f.submit '确认绑定', class: 'btn btn-primary block full-width m-b'

    %p.m-t
      %small Inspinia we app framework base on Bootstrap 3 &#169; 2015
